// swift-tools-version: 6.0
import PackageDescription

let package = Package(
    name: "SwiftCodeUtility",
    platforms: [
        .macOS(.v15)
    ],
    products: [
        .library(name: "SwiftCodeUtility", targets: ["SwiftCodeUtility"])
    ],
    dependencies: [
        // .package(url: "https://github.com/apple/swift-algorithms", from: "1.2.0")
    ],
    targets: [
        .target(
            name: "SwiftCodeUtility",
            dependencies: [],
            path: "Sources"
        ),
        .testTarget(
            name: "SwiftCodeUtilityTests",
            dependencies: ["SwiftCodeUtility"],
            path: "Tests"
        )
    ]
)
