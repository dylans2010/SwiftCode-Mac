---
Name: Logger
Category: Foundation
Description: A unified Logging service layer leveraging the Apple standard OSLog subsystem framework.
Extension: swift
---
import Foundation
import OSLog

struct __FILENAME__ {
    private static let subsystem = Bundle.main.bundleIdentifier ?? "com.swiftcode.app"

    static let general = Logger(subsystem: subsystem, category: "general")
    static let network = Logger(subsystem: subsystem, category: "network")
    static let database = Logger(subsystem: subsystem, category: "database")

    static func debug(_ msg: String) {
        general.debug("\(msg)")
    }
    static func error(_ msg: String) {
        general.error("\(msg)")
    }
}
